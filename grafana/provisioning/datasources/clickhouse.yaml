apiVersion: 1

datasources:
  - name: ClickHouse
    type: grafana-clickhouse-datasource
    access: proxy
    jsonData:
      server: clickhouse
      port: 9000
      defaultDatabase: default
      username: default
    secureJsonData:
      password: password
    isDefault: true
